{
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "apply", "tailwind", "layer", "config", "plugin", "utility", "variant", "custom-variant", "source"]
      }
    ]
  }
}
